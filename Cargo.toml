[package]
name = "SpecsUltra"
version = "0.1.0"
edition = "2024"

[dependencies]
# The core library for hardware specs
sysinfo = "0.30"
# A popular, easy-to-use GUI library
eframe = "0.24"
reqwest = { version = "0.11", features = ["blocking"] }
